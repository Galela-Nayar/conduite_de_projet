export interface project {
    id?: string;
    name: string;
    colaborateurs: string[]; // emails des collaborateurs
    taches: string[]; // Liste des tâches
    equipe : string[];// Liste des équipes
  }
  